
# Лабораторная работа №5 — Интерактивность интерфейса с использованием JavaScript

---

## Цель

Разработать **интерактивный пользовательский интерфейс** по индивидуальной предметной области (сервис №1–30), используя **чистый JavaScript без фреймворков**, реализовав:

* DOM-manipulation;
* обработку событий;
* toggle-классы;
* модальное окно;
* Tabs **или** Accordion;
* фильтрацию списка;
* динамическое добавление элементов;
* работу с `localStorage`;
* debounce для поиска.

---

# 1. Теория 

## 1.1. DOM-manipulation: что это и зачем

DOM-manipulation — это изменение HTML-структуры через JS:

* создавать/удалять элементы;
* менять текст/атрибуты;
* менять классы;
* перестраивать списки.

### Пример: изменить текст и класс

```js
const status = document.querySelector("#status");
status.textContent = "Активно";
status.classList.add("is-active");
```

### Пример: создать элемент карточки

```js
const card = document.createElement("article");
card.className = "card";
card.innerHTML = `<h3>Новая карточка</h3><p>Добавлена через JS</p>`;
document.querySelector("#list").append(card);
```

---

## 1.2. Обработка событий: click, input, submit, keydown

События — реакция интерфейса на действия пользователя.

### Пример: обработчик клика

```js
document.querySelector("#addBtn").addEventListener("click", () => {
  console.log("Кнопка нажата");
});
```

### Пример: обработчик ввода (поиск)

```js
document.querySelector("#search").addEventListener("input", (e) => {
  console.log("Пользователь ввёл:", e.target.value);
});
```

---

## 1.3. Toggle-классы: базовый механизм UI

Toggle-классы — самый частый способ “переключать состояние”:

* открыть/закрыть меню;
* выбрать карточку;
* показать/скрыть блок;
* подсветить активную вкладку.

### Пример: открыть/закрыть панель

```js
const panel = document.querySelector("#panel");
document.querySelector("#toggleBtn").addEventListener("click", () => {
  panel.classList.toggle("is-open");
});
```

---

## 1.4. Модальное окно: структура + доступность

Модалка — это окно поверх страницы. Важно:

* фон затемняется;
* фокус внутри модалки;
* закрытие по кнопке и по Esc.

### Пример HTML-структуры

```html
<button id="openModal">Открыть</button>

<div class="modal" id="modal" hidden aria-hidden="true">
  <div class="modal__content" role="dialog" aria-modal="true" aria-label="Детали">
    <button id="closeModal" aria-label="Закрыть">×</button>
    <h2>Детали</h2>
    <p>Контент модального окна</p>
  </div>
</div>
```

### Пример JS-логики

```js
const modal = document.querySelector("#modal");
const open = document.querySelector("#openModal");
const close = document.querySelector("#closeModal");

function openModal() {
  modal.hidden = false;
  modal.setAttribute("aria-hidden", "false");
}
function closeModal() {
  modal.hidden = true;
  modal.setAttribute("aria-hidden", "true");
}

open.addEventListener("click", openModal);
close.addEventListener("click", closeModal);
document.addEventListener("keydown", (e) => {
  if (e.key === "Escape" && !modal.hidden) closeModal();
});
```

---

## 1.5. Tabs / Accordion: выбор 1 из 2

### Вариант A: Tabs (вкладки)

#### HTML

```html
<div class="tabs" data-tabs>
  <div class="tabs__list" role="tablist">
    <button role="tab" data-tab="a" aria-selected="true">Описание</button>
    <button role="tab" data-tab="b" aria-selected="false">Отзывы</button>
  </div>

  <section role="tabpanel" data-panel="a">Контент A</section>
  <section role="tabpanel" data-panel="b" hidden>Контент B</section>
</div>
```

#### JS

```js
const tabs = document.querySelector("[data-tabs]");
const buttons = tabs.querySelectorAll('[role="tab"]');
const panels = tabs.querySelectorAll('[role="tabpanel"]');

buttons.forEach(btn => {
  btn.addEventListener("click", () => {
    const id = btn.dataset.tab;

    buttons.forEach(b => b.setAttribute("aria-selected", String(b === btn)));
    panels.forEach(p => p.hidden = p.dataset.panel !== id);
  });
});
```

### Вариант B: Accordion (аккордеон)

#### HTML

```html
<div class="accordion" data-acc>
  <button class="acc__btn" aria-expanded="false" data-acc-btn>Вопрос 1</button>
  <div class="acc__panel" hidden>Ответ 1</div>
</div>
```

#### JS

```js
document.querySelectorAll("[data-acc-btn]").forEach(btn => {
  btn.addEventListener("click", () => {
    const panel = btn.nextElementSibling;
    const opened = btn.getAttribute("aria-expanded") === "true";
    btn.setAttribute("aria-expanded", String(!opened));
    panel.hidden = opened;
  });
});
```

---

## 1.6. Фильтрация списка (карточек/заявок/товаров)

Фильтрация — показать только элементы, подходящие под условия.

### Пример: фильтр по категории

```js
function applyFilter(category) {
  document.querySelectorAll(".item").forEach(el => {
    const ok = category === "all" || el.dataset.category === category;
    el.hidden = !ok;
  });
}
```

---

## 1.7. Динамическое добавление элементов

Добавление карточек “на лету”: через форму “добавить”.

### Пример: добавить новую карточку

```js
function addItem(title, category) {
  const el = document.createElement("article");
  el.className = "item";
  el.dataset.category = category;
  el.innerHTML = `<h3>${title}</h3><p>Категория: ${category}</p>`;
  document.querySelector("#list").prepend(el);
}
```

---

## 1.8. localStorage: сохранение состояния интерфейса

`localStorage` хранит строковые данные (JSON).

Что хранить (пример):

* выбранный фильтр;
* открыта ли панель;
* список добавленных элементов.

### Пример: сохранить и восстановить фильтр

```js
localStorage.setItem("filter", "electronics");
const saved = localStorage.getItem("filter") || "all";
applyFilter(saved);
```

### Пример: хранить список элементов

```js
const items = JSON.parse(localStorage.getItem("items") || "[]");
items.push({ title: "Item", category: "x" });
localStorage.setItem("items", JSON.stringify(items));
```

---

## 1.9. Debounce для поиска

Debounce — “отложенный запуск” функции:

* чтобы не фильтровать на каждую букву мгновенно;
* снижает нагрузку.

### Пример debounce

```js
function debounce(fn, delay = 300) {
  let t = null;
  return (...args) => {
    clearTimeout(t);
    t = setTimeout(() => fn(...args), delay);
  };
}

const onSearch = debounce((q) => {
  q = q.trim().toLowerCase();
  document.querySelectorAll(".item").forEach(el => {
    const text = el.textContent.toLowerCase();
    el.hidden = !text.includes(q);
  });
}, 300);

document.querySelector("#search").addEventListener("input", (e) => {
  onSearch(e.target.value);
});
```

---

# 2. Задания  

---

## 2.1. Подготовка проекта 

Структура:

```
lr5/
  index.html
  css/styles.css
  js/app.js
  report/
```

---

## 2.2. DOM-manipulation 

Реализовать минимум **3 операции**:

* создание элемента (`createElement`);
* изменение текста/атрибутов;
* удаление элемента или замена контента.

---

## 2.3. Обработка событий 

Использовать минимум **4 типа событий**:

* `click`,
* `input`,
* `submit`,
* `keydown` (например Esc).

---

## 2.4. Toggle-классы 

Реализовать минимум **2 переключаемых состояния**:

* “открыто/закрыто” (панель/фильтры/меню),
* “выбрано/активно” (карточка/кнопка/таб).

---

## 2.5. Модальное окно 

Модалка должна:

* открываться по кнопке;
* закрываться по:

  * кнопке “×”,
  * клику по overlay,
  * клавише Esc;
* иметь `role="dialog"` и `aria-modal="true"`.

---

## 2.6. Tabs или Accordion (обязательно — выбрать 1)

* Tabs: минимум 2 вкладки
* Accordion: минимум 2 секции

---

## 2.7. Фильтрация списка 

Список минимум **6 элементов** (карточек/заявок/товаров).
Фильтрация минимум по **2 критериям**:

* категория (select/radio),
* поиск по тексту (input + debounce).

---

## 2.8. Динамическое добавление элементов 

Сделать форму “Добавить элемент”:

* ввод названия,
* выбор категории,
* добавление в список без перезагрузки.

---

## 2.9. localStorage 

Сохранить минимум **2 состояния**:

* список добавленных элементов,
* выбранный фильтр и/или строка поиска, и/или открытая вкладка.

При перезагрузке страницы состояние должно восстанавливаться.

---

## 2.10. Debounce для поиска 

Поиск должен использовать debounce (200–400 мс).
В отчёте кратко объяснить зачем.

---

## 2.11. Публикация результата 

* Репозиторий GitHub.
* Ссылка в отчёте.
* Скриншоты: работа фильтра, работа модалки, восстановление из localStorage.

---

# 3. Контрольные вопросы

1. Что делает `classList.toggle()`?
2. Зачем нужен debounce?
3. В каком виде лучше хранить массив объектов в localStorage?
4. Как вы обеспечили закрытие модалки по Esc?
5. Что именно вы сохранили в localStorage и почему?

---

# 4. Чек-лист для самопроверки

| Баллы | Критерии выполнения                                                                                                                                                                 |
| ----: | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     4 | Выполнены все пункты 2.1–2.11: DOM, события, toggle, модалка (3 способа закрытия), tabs/accordion, фильтры + debounce, добавление элементов, localStorage (2 состояния), публикация |
|     3 | Почти всё выполнено, но 1 ключевой пункт частично (например, нет debounce или localStorage сохраняет только 1 состояние)                                                            |
|     2 | Есть интерактивность, но не хватает системы (модалка/фильтр/сохранение неполные)                                                                                                    |
|     1 | Частичная реализация (1–2 механики)                                                                                                                                                 |
|     0 | Не соответствует заданию / нет публикации / нет работоспособности   / выполнена через ИИ                                                                                                                    |

---

### 5. Для выполнения требуется

1. **Шаблон отчёта**, указанный в описании лабораторной работы.
2. **Вариант задания**, закреплённый за студентом на семестр и опубликованный в ЛМС.
3. **Требования к оформлению** — согласно методическим указаниям в ЛМС или соответствующему разделу документации.

---

# 6. Формат сдачи работы

1. `index.html`
2. `css/styles.css`
3. `js/app.js`
4. Отчёт `.docx` или `.pdf`
5. Ссылка на GitHub-репозиторий
6. Архив `LR5_Фамилия_Группа.zip`


### 7. Запрет

❗ **Запрещается использование систем искусственного интеллекта для выполнения лабораторной работы.**

Работы, выполненные с использованием ИИ, полностью или частично скопированные, а также не сданные, оцениваются в **0 баллов** без возможности доработки.

---

## 8. Шаблон отчёта

👉 [Скачать шаблон отчёта](assets/files//LR2%20%28Shablon%29.docx)

