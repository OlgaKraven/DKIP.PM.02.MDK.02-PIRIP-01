Источник: 

---

# Лабораторная работа №1 — Применение тегов HTML при создании web-страниц

*(расширенная)*

---

## Цель

Создать статическую HTML-страницу по индивидуальной предметной области, применив расширенный набор HTML-элементов:
семантика, контент-разметка, таблицы, медиа, встраивания, интерактивные элементы, расширенная форма, доступность,
валидация, а также опубликовать результат в GitHub с предоставлением ссылки.

---

# 1. Теория (расширенная, с примерами по каждому тегу)

## 1.1. HTML-документ и роль разметки

HTML (HyperText Markup Language) — язык разметки, предназначенный для описания структуры веб-страницы. HTML-документ состоит
из разделов `head` (метаданные) и `body` (контент страницы). Корректная структура документа обеспечивает правильное отображение в
браузере, удобство сопровождения и соответствие стандартам.

HTML-документ делится на:

* `<head>` — метаданные (кодировка, заголовок вкладки, описание, иконка, настройки отображения).
* `<body>` — содержимое страницы, которое видит пользователь.

---

## 1.2. Каркас документа и метаданные (head)

### `<!DOCTYPE html>`

Назначение: включает стандартный режим HTML5 (без “quirks mode”).
Пример: всегда первая строка файла.

```html
<!DOCTYPE html>
```

### `<html lang="ru">`

Назначение: корневой элемент документа; `lang` помогает доступности и корректному выбору языка.

```html
<html lang="ru">
```

### `<head> … </head>`

Назначение: контейнер метаданных.

```html
<head>
 ...
</head>
```

### `<meta charset="utf-8">`

Назначение: кодировка, чтобы русский текст отображался корректно.

```html
<meta charset="utf-8">
```

### `<meta name="viewport" content="width=device-width, initial-scale=1.0">`

Назначение: корректное масштабирование на телефонах.

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

### `<title>`

Назначение: название вкладки браузера/закладки.

```html
<title>ServiceDesk — оформление заявки</title>
```

### `<meta name="description" content="...">`

Назначение: краткое описание страницы (часто используется в сниппетах поиска).

```html
<meta name="description" content="Страница по предметной области ServiceDesk: услуги, регламент, таблица заявок и форма обращения.">
```

### `<meta name="author" content="...">`

Назначение: автор страницы (метаданные).

```html
<meta name="author" content="Иванова А.А., ДБИ-281рсобф">
```

### `<link rel="icon" href="...">`

Назначение: иконка сайта (favicon).

```html
<link rel="icon" href="assets/favicon.ico">
```

### Дополнительные (одно на выбор)

#### `<base href="...">`

Назначение: задаёт базовый URL для относительных ссылок (аккуратно: влияет на все ссылки).

```html
<base href="/site/">
```

#### `<meta name="keywords" content="...">`

Назначение: список ключевых слов.

```html
<meta name="keywords" content="ServiceDesk, заявки, поддержка, регламент, SLA">
```

#### `<meta name="theme-color" content="#...">`

Назначение: цвет интерфейса браузера на мобильных.

```html
<meta name="theme-color" content="#dc143c">
```

---

# 1.3. Семантическая структура страницы (body)

### `<header>`

Назначение: “шапка” страницы или раздела.

```html
<header id="top">
 <h1>ServiceDesk</h1>
 <p>Портал регистрации и обработки обращений пользователей.</p>
</header>
```

### `<nav>`

Назначение: навигация по странице/сайту.

```html
<nav aria-label="Навигация по разделам">
 <ul>
 <li><a href="#about">О системе</a></li>
 <li><a href="#services">Услуги</a></li>
 <li><a href="#table">Таблица заявок</a></li>
 <li><a href="#form">Форма обращения</a></li>
 </ul>
</nav>
```

### `<main id="main">`

Назначение: уникальное основное содержимое страницы (должен быть один `main`).

```html
<main id="main">
 <!-- основной контент -->
</main>
```

### `<section>`

Назначение: логический раздел с тематикой.

```html
<section id="about">
 <h2>Описание предметной области</h2>
 <p>Система фиксирует обращения, назначает исполнителя и контролирует SLA.</p>
</section>
```

### `<article>`

Назначение: самостоятельная единица контента.

```html
<article>
 <h3>Услуга: Сброс пароля</h3>
 <p>Оформляется пользователем через форму. Исполнитель: администратор домена.</p>
</article>
```

### `<aside>`

Назначение: дополнительный контент.

```html
<aside>
 <h3>Быстрые подсказки</h3>
 <p><strong>SLA:</strong> критические заявки — до 4 часов.</p>
</aside>
```

### `<footer>`

Назначение: подвал страницы.

```html
<footer>
 <p>© 2026 Учебный проект</p>
 <address>
 Контакты: <a href="mailto:support@example.com">support@example.com</a>
 </address>
</footer>
```

---

# 1.4. Заголовки, текст и смысловые теги

### Заголовки

```html
<h1>ServiceDesk</h1>
<h2>Регламент обработки обращений</h2>
<h3>Критичность и сроки</h3>
```

### Абзац

```html
<p>Заявка — это обращение пользователя в службу поддержки с описанием проблемы.</p>
```

### `<br>`

```html
<p>График работы: Пн–Пт<br>09:00–18:00</p>
```

### `<hr>`

```html
<hr>
```

### `<strong>`

```html
<p><strong>Важно:</strong> заполните тему заявки и контактный телефон.</p>
```

### `<em>`

```html
<p>В поле “Описание” укажите <em>шаги воспроизведения</em> проблемы.</p>
```

### `<mark>`

```html
<p>Статус <mark>Критический</mark> требует обработки в приоритетном порядке.</p>
```

### `<small>`

```html
<small>Примечание: срок SLA может отличаться по договору.</small>
```

### `<sub>` и `<sup>`

```html
<p>Показатель доступности: 99.9<sup>%</sup></p>
<p>Формула: H<sub>2</sub>O</p>
```

### `<abbr>`

```html
<p><abbr title="Service Level Agreement">SLA</abbr> — целевой уровень сервиса.</p>
```

### `<time>`

```html
<p>Последнее обновление регламента: <time datetime="2026-02-17">17.02.2026</time></p>
```

### `<cite>`

```html
<p>Источник: <cite>Регламент обработки обращений, версия 1.0</cite></p>
```

### `<q>`

```html
<p>Пользователь сообщил: <q>Не могу войти в систему после обновления</q>.</p>
```

### `<blockquote>`

```html
<blockquote>
 При критической заявке необходимо немедленно назначить ответственного и уведомить пользователя о сроках.
</blockquote>
```

### `<pre><code>`

```html
<pre><code>&lt;form method="post" action="#"&gt;
 &lt;input type="email" required&gt;
&lt;/form&gt;</code></pre>
```

### `<kbd>`

```html
<p>Откройте поиск на странице: <kbd>Ctrl</kbd> + <kbd>F</kbd>.</p>
```

### `<samp>`

```html
<p>Сообщение системы: <samp>Заявка #1042 создана</samp>.</p>
```

### `<var>`

```html
<p>Пусть <var>t</var> — время реакции, тогда SLA выполняется при <var>t</var> ≤ 4 часа.</p>
```

### `<span>`

```html
<p>Статус: <span>В работе</span></p>
```

### `<div>`

```html
<div>
 <p>Этот блок объединяет элементы карточки без отдельного семантического смысла.</p>
</div>
```

---

# 1.5. Списки

### `<ul>`

```html
<ul>
 <li>Пользователь</li>
 <li>Оператор</li>
 <li>Администратор</li>
</ul>
```

### `<ol>`

```html
<ol>
 <li>Пользователь создаёт заявку</li>
 <li>Система присваивает номер</li>
 <li>Оператор классифицирует</li>
 <li>Назначается исполнитель</li>
 <li>Заявка закрывается с результатом</li>
</ol>
```

### Вложенный список

```html
<ul>
 <li>Инциденты
  <ul>
   <li>Сбой доступа</li>
   <li>Ошибка приложения</li>
  </ul>
 </li>
</ul>
```

### `<dl>`

```html
<dl>
 <dt>Инцидент</dt>
 <dd>Непредвиденный сбой, нарушающий работу сервиса.</dd>
</dl>
```

---

# 1.6. Ссылки

### Внешняя

```html
<a href="https://validator.w3.org/" target="_blank" rel="noopener noreferrer">
 Валидатор HTML
</a>
```

### Внутренняя

```html
<a href="#form">Перейти к форме</a>
```

### Наверх

```html
<a href="#top">Наверх</a>
```

### mailto

```html
<a href="mailto:support@example.com">support@example.com</a>
```

### tel

```html
<a href="tel:+79990001122">+7 (999) 000-11-22</a>
```

---

# 1.7. Изображения и медиа

```html
<img src="assets/logo.png" alt="Логотип ServiceDesk" width="140">
<img src="assets/scheme.png" alt="Схема обработки заявки" loading="lazy" width="600">
```

### `<figure>`

```html
<figure>
 <img src="assets/scheme.png" alt="Схема процесса обработки заявки" width="600">
 <figcaption>Рисунок 1 — Схема обработки заявки</figcaption>
</figure>
```

### `<picture>`

```html
<picture>
 <source srcset="assets/banner.webp" type="image/webp">
 <img src="assets/banner.jpg" alt="Баннер портала заявок" width="800">
</picture>
```

### `<audio>`

```html
<audio controls>
 <source src="assets/notification.mp3" type="audio/mpeg">
</audio>
```

### `<video>`

```html
<video controls width="640">
 <source src="assets/demo.mp4" type="video/mp4">
</video>
```

---

# 1.8. Встраивания

### `<iframe>`

```html
<iframe
 src="https://www.openstreetmap.org/export/embed.html?bbox=..."
 width="600" height="350"
 title="Карта расположения офиса">
</iframe>
```

### `<embed>`

```html
<embed src="assets/reglament.pdf" type="application/pdf" width="600" height="400">
```

### `<object>`

```html
<object data="assets/reglament.pdf" type="application/pdf" width="600" height="400">
 <p>PDF не отображается. Скачайте файл</p>
</object>
```

### `<noscript>`

```html
<noscript>
 <p><strong>Примечание:</strong> В вашем браузере отключён JavaScript.</p>
</noscript>
```

### `<canvas>`

```html
<canvas width="300" height="120">
 Ваш браузер не поддерживает canvas.
</canvas>
```

### `<svg>`

```html
<svg width="24" height="24" aria-label="Иконка заявки" role="img">
 <circle cx="12" cy="12" r="10"></circle>
</svg>
```

---

# 1.9. Таблица

```html
<table>
 <caption>Таблица заявок</caption>
 <thead>
  <tr>
   <th scope="col">№</th>
   <th scope="col">Тема</th>
   <th scope="col">Статус</th>
   <th scope="col">Срок (SLA)</th>
  </tr>
 </thead>
 <tbody>
  <tr>
   <th scope="row">1042</th>
   <td>Сброс пароля</td>
   <td>В работе</td>
   <td>4 часа</td>
  </tr>
 </tbody>
 <tfoot>
  <tr>
   <td colspan="4">Итого заявок: 1</td>
  </tr>
 </tfoot>
</table>
```

---

# 1.10. Форма (расширенная)

```html
<form id="form" method="post" action="#">
 <fieldset>
  <legend>Данные пользователя</legend>

  <label for="fullname">ФИО</label>
  <input id="fullname" name="fullname" type="text" required>

  <label for="email">Email</label>
  <input type="email" id="email" required>

  <label for="phone">Телефон</label>
  <input type="tel" id="phone" pattern="^\+?\d{10,15}$" required>

 </fieldset>

 <fieldset>
  <legend>Данные заявки</legend>

  <label for="desc">Описание</label>
  <textarea id="desc" required minlength="30" maxlength="500"></textarea>

  <label>
   <input type="checkbox" required> Согласен с обработкой данных
  </label>

 </fieldset>

 <button type="submit">Отправить</button>
 <button type="reset">Очистить</button>
</form>
```

---

# 2. Публикация результата

### Структура репозитория

```
index.html
assets/
report/
```

### Обязательно

* Проверка через W3C Validator
* Исправление ошибок
* Скриншот проверки
* Публикация на GitHub
* Ссылка в отчёте

---

# 3. Контрольные вопросы

### Закрытого типа

1. Какой тег предназначен для основного уникального содержимого страницы?
   **Ответ: B) `<main>`**

2. Какой тег связывает изображение и подпись?
   **Ответ: B) `<figcaption>`**

### Открытого типа

1. Чем отличается `<section>` от `<article>`?
2. Зачем нужны `<thead>/<tbody>/<tfoot>`?
3. Что даёт связка `<label for>` + `id`?
4. В чём преимущество `<picture>` над одиночным `<img>`?
5. Когда уместны `<details>/<summary>`?

---

# 4. Чек-лист (0–4 балла)

| Баллы | Критерии выполнения                           |
| ----- | --------------------------------------------- |
| 4     | Выполнены все пункты 2.1–2.12                 |
| 3     | Почти всё выполнено                           |
| 2     | Выполнены базовые разделы                     |
| 1     | Страница частично выполнена                   |
| 0     | Работа скопирована или выполнена с помощью ИИ |

---

# 5. Формат сдачи работы

1. `index.html`
2. Отчёт `.docx` или `.pdf`
3. Ссылка на GitHub-репозиторий
4. Архив `LR1_Фамилия_Группа.zip`


## 6. Шаблон отчёта
👉 [Скачать шаблон отчёта](assets/files//LR2%20%28Shablon%29.docx)
